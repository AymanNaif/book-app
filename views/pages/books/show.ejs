<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Details</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h2>Best Book</h2>
        <h3 style="text-align: center; margin-top: 35px;">Find Your Favourit Book</h3>
        <Button><a href="/" style="text-decoration: none;color: white; font-size: 20px;">Home</a></Button>
        <Button><a href="/search" style="text-decoration: none;color: white; font-size: 20px;">Search Your Book</a></Button>
      </header>
    <main>
        
<% dataBaseData.forEach((element)=>{ %> 
    <div class="books">
       <img src="<%= element.img_url %>" alt="">
       <h3><%= element.title %></h3>
       <h4><%= element.authors %></h4>
       <p><%= element.description %></p>
         <h5>ISBN is: <%= element.isbn %></h5>
      
         <form action="/deleteBook/<%= element.id %>?_method=delete" method="post" >
         <button>DELETE</button>
      </form>

         <button id="update">UPDATE</button>
         <div style="display: none;" class="updateForm">
          <h5>Update book data:</h5>
         <form action="/updateBook/<%= element.id %>?_method=put" method="post" >
          <input  type="text"  name="img_url" id="img_url" value="<%= element.img_url %>"> <br>
          <input  type="text" name="title" id="title" value="<%= element.title %>"><br>
         <input  type="text" name="authors" id="author" value="<%= element.authors %>"><br>
         <input  type="text" name="description" id="description" value="<%= element.description %>"><br>
         <input  type="text" name="isbn" id="isbn" value="<%= element.isbn %>"><br>
 
         <button style="margin-bottom: 10px; ">Make Update</button>
      </form>
    </div>
  
   </div>
  <% }) %>
    </main>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/app.js"></script>
</body>
</html>